# Redis persistence configuration for media-platform
# Loaded via docker-compose volume mount

# AOF (Append Only File) — primary persistence
appendonly yes
appendfsync everysec

# RDB snapshots — backup safety net
save 900 1
save 300 10
save 60 10000

# Max memory policy
maxmemory 128mb
maxmemory-policy noeviction
